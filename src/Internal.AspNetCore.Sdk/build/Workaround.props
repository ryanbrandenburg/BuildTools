<!--
WARNING: These targets are intended for building Microsoft's ASP.NET Core repos, and is not intended
for use outside of Microsoft.

This file is dedicated to working around issues in Microsoft.NET.Sdk
-->
<Project>
  <!-- Workaround for https://github.com/dotnet/standard/issues/200 -->
  <PropertyGroup>
    <DisableHandlePackageFileConflicts Condition="!$(TargetFramework.StartsWith('netcoreapp'))">true</DisableHandlePackageFileConflicts>
  </PropertyGroup>
</Project>
